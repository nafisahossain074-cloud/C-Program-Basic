#include <stdio.h>
int main() {
    float a1, b1, a2, b2;
    printf("Enter first complex number (a b): ");
    scanf("%f %f", &a1, &b1);
    printf("Enter second complex number (a b): ");
    scanf("%f %f", &a2, &b2);
    printf("Sum = %f + %fi", a1 + a2, b1 + b2);
    return 0;
}

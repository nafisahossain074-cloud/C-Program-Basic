#include <stdio.h>
int main() {
    float l, w;
    printf("Enter length and width: ");
    scanf("%f %f", &l, &w);
    printf("Area = %f\n", l * w);
    printf("Perimeter = %f", 2 * (l + w));
    return 0;
}
